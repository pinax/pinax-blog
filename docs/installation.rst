.. _installation:

============
Installation
============

* Install the development version::

    pip install pinax-blog

* Add `pinax-blog` to your `INSTALLED_APPS` setting::

    INSTALLED_APPS = (
        # ...
        "pinax.blog",
        # ...
    )


.. _dependencies:

Dependencies
============

